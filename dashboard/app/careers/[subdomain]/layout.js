import '../../../src/careers/careers.css';

export const metadata = {
  title: 'Careers — intervieHire',
  description: 'Explore open roles and apply.',
};

export default function CareersLayout({ children }) {
  return <>{children}</>;
}
